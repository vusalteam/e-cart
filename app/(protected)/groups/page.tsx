import {Button} from "@/components/ui/button";
import {BasePage} from "@/app/(protected)/_components/base-page";

const Page = () =>{
    return (
        <BasePage title={'Группы'} button={<Button variant='cyan'>Создать группу</Button>}>
            sfa
        </BasePage>
    )
}
export default Page